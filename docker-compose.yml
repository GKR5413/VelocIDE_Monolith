services:
  velocide:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: velocide-monolith
    ports:
      - "8080:8080"
    environment:
      - PORT=8081
      - WORKSPACE_ROOT=/app/workspace
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
    restart: unless-stopped
